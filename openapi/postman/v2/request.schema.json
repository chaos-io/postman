{
    "title": "postman.v2.Request",
    "description": "",
    "type": "object",
    "properties": {
        "body": {
            "title": "mojo.core.Array",
            "description": "This field contains the data usually contained in the request body.",
            "type": "array",
            "items": {
                "$ref": "#/components/schemas/postman.v2.Request.Body"
            }
        },
        "description": {
            "$ref": "#/components/schemas/postman.v2.Description"
        },
        "header": {
            "title": "mojo.core.Array",
            "description": "A representation for a list of headers",
            "type": "array",
            "items": {
                "$ref": "#/components/schemas/postman.v2.Request.Header"
            }
        },
        "method": {
            "title": "mojo.core.String",
            "description": "GET PUT POST PATCH DELETE COPY HEAD OPTIONS LINK UNLINK PURGE LOCK UNLOCK PROPFIND VIEW",
            "type": "string"
        },
        "url": {
            "title": "mojo.core.Array",
            "type": "array",
            "items": {
                "$ref": "#/components/schemas/postman.v2.Url"
            }
        }
    }
}